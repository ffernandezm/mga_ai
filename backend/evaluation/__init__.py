"""Framework de evaluación controlada del asistente LLM-RAG de MGA_IA.

Independiente del proveedor: usa la configuración existente de `LLMManager`.
El modo por defecto del runner es `--dry-run` (no invoca ningún proveedor).
"""
